import AppLayout from '../../../src/containers/layout/app';

const BasicPositionList = () => {
  return (
    <AppLayout>
      {`기초설정 > 부서/직원 > 직책정보`}
    </AppLayout>
  );
};

export default BasicPositionList;
