import AppLayout from '../../../src/containers/layout/app';

const SysCompanyList = () => {
  return (
    <AppLayout>
      {`환경설정 > 권한관리 > 회사관리`}
    </AppLayout>
  );
};

export default SysCompanyList;
